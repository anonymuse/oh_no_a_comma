# Anonymized demo notes

This project is a synthetic frontend demo. It does not document a real company, domain, API, screenshot, ATS vendor, or job listing.

The walkthrough demonstrates a general failure class: raw freeform locality strings can produce duplicate public filter options and fragmented matching when the UI renders and filters by raw strings instead of canonical keys.
