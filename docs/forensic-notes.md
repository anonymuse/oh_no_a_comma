# Anonymized demo notes

This project is a synthetic frontend demo. It does not document a real company, domain, API, screenshot, or job listing.

The walkthrough uses a mock payload to demonstrate a general class of defect: raw freeform location strings can produce duplicate public filter options when the UI renders them without canonicalization.
